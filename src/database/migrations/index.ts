// import { SchemaMigrations } from '@nozbe/watermelondb';

// const migrations = SchemaMigrations({ migrations: [] });

// export default migrations;

// Temporary mock migrations for development
const migrations = {
  // Mock migrations will be replaced with real backend integration
};

export default migrations;